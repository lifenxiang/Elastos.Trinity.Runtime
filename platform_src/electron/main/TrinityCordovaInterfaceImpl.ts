import { WebViewFragment } from './WebViewFragment';

export class TrinityCordovaInterfaceImpl {
    public fragment: WebViewFragment;

    constructor(fragment: WebViewFragment) {
        this.fragment = fragment;
    }
}